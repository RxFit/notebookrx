import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/context/AuthContext";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "NotebookLM Clone — AI Research Assistant",
  description: "Deterministic RAG-based AI assistant. Upload documents, ask questions grounded in your sources. Zero hallucinations.",
  keywords: "AI, research, RAG, document analysis, NotebookLM",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
